<UserControl x:Class="NothingVpn.Desktop.Wpf.ProfilesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="22" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid>
            <StackPanel>
                <TextBlock Text="Профили" FontSize="28" FontWeight="SemiBold" />
                <TextBlock Text="Сохранённые подключения VLESS" Foreground="{StaticResource MutedBrush}" Margin="0,5,0,0" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Button Content="Подписки" Click="OnSubscriptions" Background="#EEF0F4" Foreground="{StaticResource TextBrush}" Margin="0,0,10,0" />
                <Button Content="Добавить профиль" Command="{Binding AddCommand}" />
            </StackPanel>
        </Grid>

        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Padding="0">
            <Grid>
                <Grid.RowDefinitions><RowDefinition Height="*" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
                <ListBox ItemsSource="{Binding Profiles}" SelectedItem="{Binding SelectedProfile}" BorderThickness="0"
                         Background="Transparent" Padding="8" MouseDoubleClick="OnProfileDoubleClick">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="10,9" MinHeight="44">
                                <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold" />
                                    <TextBlock Foreground="{StaticResource MutedBrush}" FontSize="12" Margin="0,4,0,0">
                                        <Run Text="{Binding Host}" /><Run Text=":" /><Run Text="{Binding Port}" />
                                    </TextBlock>
                                </StackPanel>
                                <Border Grid.Column="1" Background="#EEF0F4" CornerRadius="8" Padding="9,5" VerticalAlignment="Center">
                                    <TextBlock FontSize="11" Foreground="{StaticResource MutedBrush}">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Text" Value="Подписка" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding SubscriptionId}" Value="{x:Null}">
                                                        <Setter Property="Text" Value="Вручную" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Border>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                <Border Grid.Row="1" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0" Padding="14">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Удалить" Command="{Binding DeleteCommand}" Background="#FFF0F0" Foreground="#B42318" Margin="0,0,10,0" />
                        <Button Content="Изменить" Command="{Binding EditCommand}" Background="#EEF0F4" Foreground="{StaticResource TextBrush}" />
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>
