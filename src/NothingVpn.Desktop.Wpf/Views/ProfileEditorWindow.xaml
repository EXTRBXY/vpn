<Window x:Class="NothingVpn.Desktop.Wpf.ProfileEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Профиль" Width="620" Height="360" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False" Icon="../Assets/app.ico">
    <Grid Margin="26">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" /><RowDefinition Height="20" /><RowDefinition Height="Auto" />
            <RowDefinition Height="16" /><RowDefinition Height="Auto" /><RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="Heading" FontSize="24" FontWeight="SemiBold" />
        <StackPanel Grid.Row="2">
            <TextBlock Text="Название" Foreground="{StaticResource MutedBrush}" FontSize="12" />
            <TextBox x:Name="NameBox" Margin="0,7,0,0" Padding="11,9" />
        </StackPanel>
        <StackPanel Grid.Row="4">
            <TextBlock Text="VLESS-ссылка" Foreground="{StaticResource MutedBrush}" FontSize="12" />
            <TextBox x:Name="LinkBox" Margin="0,7,0,0" Padding="11,9" TextWrapping="NoWrap" />
            <TextBlock x:Name="ErrorText" Foreground="#B42318" Margin="0,9,0,0" TextWrapping="Wrap" Visibility="Collapsed" />
        </StackPanel>
        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Отмена" Click="OnCancel" Background="#EEF0F4" Foreground="{StaticResource TextBrush}" Margin="0,0,10,0" />
            <Button Content="Сохранить" Click="OnSave" />
        </StackPanel>
    </Grid>
</Window>
